import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

Widget sidePadding05({required BoxConstraints constraints}) {
  return SizedBox(
    width: constraints.maxWidth * 0.05,
  );
}
